[project]
name = "admin-models"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "sqlalchemy>=2.0.0",
    "asyncpg>=0.27.0",
    "pydantic>=2.0",
    "psycopg2-binary>=2.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
